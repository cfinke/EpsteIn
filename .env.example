# Comma-separated list of explicit frontend origins allowed by CORS.
# Example: http://localhost:3000,https://app.yourdomain.com
CORS_ALLOW_ORIGINS=http://localhost:3000

# Required bearer token for POST /search and POST /report.
# Use a long random secret in real deployments.
API_BEARER_TOKEN=replace-with-a-long-random-secret

# Host port published by docker-compose (container still listens on 8000).
# Change this if you have a port conflict.
API_HOST_PORT=8010

# Maximum time budget (seconds) per /search and /report request before the API
# returns partial results. Helps avoid proxy timeouts (for example Cloudflare 524).
SEARCH_MAX_DURATION_SECONDS=85

# Timeout (seconds) for each upstream lookup request.
UPSTREAM_TIMEOUT_SECONDS=15
