# Comma-separated list of explicit frontend origins allowed by CORS.
# Example: http://localhost:3000,https://app.yourdomain.com
CORS_ALLOW_ORIGINS=http://localhost:3000

# Required bearer token for POST /search and POST /report.
# Use a long random secret in real deployments.
API_BEARER_TOKEN=replace-with-a-long-random-secret
